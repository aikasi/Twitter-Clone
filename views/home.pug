extends base.pug

block content
    h4=user.id
    h4=user.email
    h4=user.password
    h4=user.nick
    h3 Tweet
    each info in user.tweet
        h4=info.id
    a(href=`${routes.tweet}${routes.tweetCreate}`) Tweet